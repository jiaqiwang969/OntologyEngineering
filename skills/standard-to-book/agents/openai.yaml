interface:
  display_name: "Standard to Book"
  short_description: "Build books bound to Semantica-only semantics"
  default_prompt: "Use $standard-to-book to create a privacy-safe, source-grounded book and bind all executable knowledge to one built-in Semantica package."
