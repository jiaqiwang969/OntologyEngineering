# Semantica is the sole executable semantic implementation.  The matching
# source-built wheel is vendored and pinned by semantica-source-lock.json.
--find-links runtime/vendor
semantica[ontology-runtime]==0.6.5+oe.4
pytest>=8.0
