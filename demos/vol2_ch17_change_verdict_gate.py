"""第二卷变更判决佐证；可执行资产由 Semantica 独占。"""

import sys
from _common import run_package_demo

sys.exit(run_package_demo(
    "semantica.chapter_packages.vol2.ch17",
    claim="变更影响与保留判决必须带理由，缺失判决时门禁拒绝。",
    source_anchor="《产品可信工程》ch17",
))
