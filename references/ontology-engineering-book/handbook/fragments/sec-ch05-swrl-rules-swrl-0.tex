\begin{codebox}
\codeline{\textcolor{cmtgray}{\#\ SWRL\ 规则示例\ -\ 工程领域推理规则}}
\codeline{\textcolor{cmtgray}{\#\ SWRL\ Rules\ -\ Engineering\ Domain\ Inference\ Rules}}
\codeline{\textcolor{cmtgray}{\#}}
\codeline{\textcolor{cmtgray}{\#\ SWRL\ (Semantic\ Web\ Rule\ Language)\ 结合了OWL的类定义和规则推理}}
\codeline{\textcolor{cmtgray}{\#\ 语法：前件(Antecedent)\ \(\rightarrow\)\ 后件(Consequent)}}
\codeline{\textcolor{cmtgray}{\#\ 多个原子之间用\ \(\land\)\ (AND)\ 连接}}
\codeline{\textcolor{cmtgray}{\#}}
\codeline{\textcolor{cmtgray}{\#\ 注意：标准SWRL不支持规则体中的否定（\(\lnot\)）。}}
\codeline{\textcolor{cmtgray}{\#\ 能力冲突规则中的"设备无法加工"需借助外部DL-Safe规则扩展实现，}}
\codeline{\textcolor{cmtgray}{\#\ 或在应用层通过SPARQL查询辅助判断。}}
\codeline{\textcolor{cmtgray}{\#\ Note:\ Standard\ SWRL\ does\ not\ support\ negation-as-failure\ in\ rule\ bodies.}}
\codeline{\textcolor{cmtgray}{\#\ Capability\ conflict\ rules\ requiring\ "equipment\ cannot\ process"\ must\ be\ handled}}
\codeline{\textcolor{cmtgray}{\#\ via\ DL-Safe\ rule\ extensions\ or\ supplementary\ SPARQL\ queries\ at\ the\ application}}
\codeline{\hspace*{4\ttcw}layer.}
\codeblank{}
\end{codebox}
