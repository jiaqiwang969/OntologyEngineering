# CAD Agent 并入工程本体论：本地实施状态

## 2026-09-25 当前操作路线

CAD 默认改为直接 NXOpen/Journal，标准件默认米思米中国。setup/doctor/能力目录均已迁移，旧 Fusion/MCP 入口为显式历史兼容，不能自动回退。完整机械工程方法、A0–A8/S0–S11、运动学、接触物理及证据交接保留。详见 [迁移映射](../skills/cad-agent/references/nx-method-migration.md)。下文旧日期接入记录保留其历史范围，不能当作当前默认。此次不发布公共包，不改变 Semantica 正式语义源锁。

2026-09-22。此记录区分技能入口、运行进程、语义裁定和产品放行；本地打包通过不等于正式 Semantica release 或客户交付批准。

## 已实现的协同关系

`skills/cad-agent/` 是新的 CAD 模块正本，根 `SKILL.md` 与制造工艺模块双向路由。CAD 模块负责 Fusion／NX／AutoCAD 的原生几何和回读证据、对象身份、界面及装配状态；制造模块负责工艺功能、失效路径、检验、成本和交期的因果审查。`cad_process_handoff.py` 校验来源文件 SHA-256、原生回读 JSON 指针、模型内嵌来源摘要和对象依赖，输出项目 ABox、CAD→工艺／工艺→CAD 问题及变更影响清单。证据投影只写确定性的 N-Triples，不运行查询、SHACL 或规则；正式语义只由根控制面中的 source-locked Semantica 执行。

旧 CAD 本体、SPARQL、SHACL 和雨刮示例验证器共 190 个文件已从新模块的活动源码面移出，原字节保存在私有迁移归档及旧 CAD 目录。新模块的 `semantic_query.py capabilities` 只读取静态工具清单，不执行旧本体查询。原 CAD wheel 确曾内含可执行 SHACL/SPARQL 的 MCP 侧车；新模块现只分发从锁定来源提取的 Fusion 执行 wheel，不含 `cad_agent`、`cad_geometry_mcp`、本体资产和 `cad-agent-mcp` 入口。打包前和安装后均核对 wheel 摘要、成员与入口。CAD 本地工具运行所需的动态加载和子进程调用由 `runtime/cad-operational-source-lock.json` 对 31 个确切文件逐一锁定 SHA-256；改动后门禁要求重审。锁不豁免显式本体引擎调用、第二套语义资产或直接导入语义后端。

真实项目首例及其图纸、参数、人物和试跑结果留在仓外受控目录；本模块只保留通用交接合同、适配器与合成反例。该首例用于核对 CAD 来源、必要功能、候选工艺覆盖和下游决定之间的依赖，不能将作者试跑当作产品结论。

## 已验证的本地边界

- CAD、制造和根技能的结构校验通过；新 CAD `doctor.sh --json` 为 `ready=true`（只读预检，未发送 Fusion MCP 请求）。
- CAD 交接的 6 项测试和工艺投影的 3 项测试通过；包含来源篡改、原生回读漂移、对象错引、影响传播及未验证覆盖不能升级的反例。
- Semantica 唯一后端门禁的严格模式为 0 个剩余发现；另有 31 个按文件哈希锁定的非语义 CAD 执行源和独立 wheel 成员审查。门禁的新增反例证明改动锁定文件、篡改 wheel、重新锁定含语义引擎的 wheel 或显式调用本体引擎均会失败。
- `scripts/package_skill.py` 对整根目录的文件闭合、直接个人路径和传输锁检查通过；独立解包复验另记在分发记录中。

## 迁移时保留的状态（2026-09-22）

旧顶层 `cad-agent` 已改名为 `cad-agent-legacy` 技能入口以免与新模块重复命名。旧运行目录暂留给已有会话；路径切换应在无活跃调用时重新检查，并做旧命令与新入口的只读对照。

2026-09-24 接入补记：新增[可选 MCP 传输](../skills/cad-agent/references/mcp-registration.md)，Fusion 保留每次调用一个 guarded cell，语义工具转交本根目录锁定的 Semantica CLI。当前工作站已将旧 MCP 注册退役，停止其旧语义服务进程，并按用户要求将旧独立目录完整移出技能目录归档；归档校验与配置备份保留在用户本地。初始化与工具列表、Semantica doctor/discover 已验证。Fusion 原生调用仍受已审核版本目录限制，工具可列出不表示所有已安装 Fusion 版本均可执行。

制造因果关口的 Semantica 后继仍是候选。没有项目已采用的 `ProjectOntologyBinding` 与 fresh task 时，只能形成来源完整性投影和候选作者试跑，不能签发正式项目 review receipt、release 或 promotion。真实产品的物理闭合另需同版结构、代表性截面及适用的检漏、承压等原生证据。

## 2026-09-24 兼容修复与方法入口补记

Fusion `2705.1.15` 已完成原生目录及固定只读行为核验，版本化为 Fusion-only runtime
`0.1.0+oe.2`。正式注册入口的项目读取，以及 canonical cell 的打开文档列表和只读
Python 脚本均返回 `delivered_ok`、ACK 已确认；当前文档已保存且未修改。此验证未创建
或修改几何。守卫与会话代码未变，未知版本仍拒绝。详见
[版本兼容记录](../skills/cad-agent/references/fusion-release-compatibility.md)。

工程本体根入口、展示说明与分享版入口已突出功能本体、第一性原理和工艺本体：从
服务对象与必要功能，经过物理约束推导结构、公差及验证，再联动 cad-agent 的原生
CAD 与制造工艺、装夹、焊接、检验和成本。方法要求与实际可执行本体覆盖分别陈述；
未实现的规则仍是候选，具体项目不因这次入口修订取得正式语义或产品放行。


## 2026-09-24 多实例与后台控制补记（第一阶段，R1）

CAD 模块新增了[多实例与后台控制入口](../skills/cad-agent/references/fusion-multi-instance.md)。
调用可按实际 loopback 端点、PID 与精确启动令牌冻结到独立进程；窗口与截图使用
独立 CGWindow 属主证据，同 PID 的 owner lease、保存义务和共享安全状态保留。
新版本 2705.1.25 的 catalog/projects 固定审计通过后纳入运行时；同一实例已完成
独立测试文档的创建、保存与 v2 原生几何回读。该示例为 12 个台阶销，逐体尺寸与
解析体积均匹配。它验证软件接口，不能代替生产夹具或工艺方案的验收。

用户要求避免前台焦点干扰，已写入默认控制规则：优先 Peekaboo 按精确 PID／窗口
进行后台观察；后台控件不可操作时不自动升级到前台点击。单独评估的 Peekaboo 4.5.0
能完成受身份约束的后台截图，但本机 Fusion 首选项按钮的后台 AXPress 未成功，
随后保持外部取证，没有重复点击。该阶段双 Agent 同时建模尚未验收；后续 R2 结果见下。

社区源码调研保留固定 commit／许可证与适用边界；采用哪一部分代码与上游宣称
能力分开记录。本轮没有恢复 CAD 独立语义后端，也没有形成新的项目语义发布。

运行时 oe.7 与外部预检补上确切 `Preferences`／`首选项` 面板标题的上游连接前拦截，
49 项针对性离线测试通过，保存与实例隔离方法保持原字节。安装后仅用 OS／Peekaboo
外部证据复查：A 因首选项被拦截，B 就绪；没有为验证拦截而发送原生请求。

## 2026-09-24 双 Agent 原生建模验收完成（R2）

实际两个 Agent、两个独立 Fusion 进程、两个独立文档已通过两轮建模／保存／全新
请求回读。每侧首轮 96 个实体、次轮新增 48 个，最终各 144 个实体、288 个草图和
288 次拉伸；保存版本依次为 v2、v3，均 clean。原生建模时间真实重叠 55.708 秒和
69.064 秒，且两边在重叠区间都有实际特征进展；八次调用均首次成功、ACK 已确认。
逐体尺寸／位置／体积／名称集合通过，前 96 个实体和原有项目文件的几何摘要保持
不变。前台采样只有用户终端，没有 Fusion 前台样本。

运行时更新至 `0.1.0+oe.9`：后台一次准确按钮操作后，重新证明已关闭首选项的保留
窗口状态；只对完整身份与排序证据支持的共同坐标平移做有界重新取证，最终坐标仍须
精确一致。保护类、保存、互斥和退役机制保留，无前台回退或新增持久豁免。
21 项回归和 12 个独立边界用例通过，两次构建一致，已安装 36 个包成员逐字节核验。
最终 Semantica strict 扫描 300 个文件，剩余发现为 0；非语义执行源锁为 36 条。

本机证据包 `fusion-parallel-acceptance-20260924-r2` 的结果为 `PASS_TWO_ROUNDS`。
可据此安排独立零件并行建模，仍不能推定同一文件并发编辑、复杂装配、CAM、长期
无人值守或生产夹具已验收。没有串行基准，不报告加速倍数。本轮为执行能力验收，
工程知识变更 `no_delta`，不产生项目语义 review 或产品放行。
