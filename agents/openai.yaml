interface:
  display_name: "Engineering Ontology"
  short_description: "Ground ontology answers in the local book"
  default_prompt: "Use $ontology-engineering to explain an engineering ontology concept from the local book and relate it to a PDE-agent example."
