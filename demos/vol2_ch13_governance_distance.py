"""第二卷治理距离佐证；可执行资产由 Semantica 独占。"""

import sys
from _common import run_package_demo

sys.exit(run_package_demo(
    "semantica.chapter_packages.vol2.ch13",
    claim="治理角色与确认独立性必须转化为可执行、可拒绝的距离约束。",
    source_anchor="《产品可信工程》ch13",
))
